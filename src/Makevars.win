## Include TMB headers (which includes Eigen) on Windows
## R automatically adds TMB include path when TMB is in LinkingTo,
## but we ensure it's explicitly set for Windows
PKG_CPPFLAGS = $(shell "$(R_HOME)/bin$(R_ARCH_BIN)/Rscript.exe" --vanilla -e "cat(paste0('-I', system.file('include', package='TMB')))")

## Standard C++ flags for TMB
CXX_STD = CXX17
