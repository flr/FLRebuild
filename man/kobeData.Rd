% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pMetricsFuncs.R
\name{kobeData}
\alias{kobeData}
\alias{dtK}
\title{Create Kobe Data Frame with Probabilities}
\usage{
kobeData(stock, harvest, ...)

dtK(...)
}
\arguments{
\item{stock}{Numeric vector, FLQuant, or data.frame containing relative stock biomass (B/BMSY)}

\item{harvest}{Numeric vector, FLQuant, or data.frame containing relative harvest (F/FMSY)}

\item{...}{Additional arguments passed to kobeSummary}
}
\value{
A data.frame with columns:
  \item{stock}{Relative stock biomass (B/BMSY)}
  \item{harvest}{Relative harvest rate (F/FMSY)}
  \item{red}{Probability of being in red quadrant}
  \item{green}{Probability of being in green quadrant}
  \item{yellow}{Probability of being in yellow quadrant}
  \item{orange}{Probability of being in orange quadrant}
  \item{overFished}{Probability that stock is overfished}
  \item{overFishing}{Probability that overfishing is occurring}
  \item{year}{Year (if available)}
}
\description{
Creates a data frame with Kobe plot data including quadrant probabilities.
This is a convenience wrapper around kobeSummary that returns the full
data frame with probabilities.
}
\details{
This function creates a data frame suitable for Kobe plot analysis.
It combines stock and harvest data with quadrant probabilities calculated
using the prob() function.
}
\examples{
\dontrun{
# From FLQuant objects
stock_rel <- ssb(ple4) \%/\% refpts(ple4brp)["msy", "ssb"]
harvest_rel <- fbar(ple4) \%/\% refpts(ple4brp)["msy", "harvest"]
dtK <- kobeData(stock_rel, harvest_rel)

# From data.frame
df <- data.frame(stock = c(0.8, 1.2), harvest = c(0.9, 1.1), year = 2010:2011)
dtK <- kobeData(df)
}

}
