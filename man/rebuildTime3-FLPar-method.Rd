% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rebuild_biodyn.R
\name{rebuildTime3,FLPar-method}
\alias{rebuildTime3,FLPar-method}
\title{Calculate Rebuild Time using FLPar parameters (Alternative Implementation)}
\usage{
\S4method{rebuildTime3}{FLPar}(object, nyrs = 50, ftar = 0, timing = 0, ...)
}
\arguments{
\item{object}{An FLPar object containing parameters (r, p, bmsy, fmsy, virgin)}

\item{nyrs}{Number of years for projection (default = 50)}

\item{ftar}{Target fishing mortality multiplier (default = 0.0 for no fishing)}

\item{timing}{Timing parameter (default = 0)}

\item{...}{Additional arguments}
}
\value{
A data frame with columns:
  \item{initial}{Initial depletion level relative to BMSY}
  \item{T_recover}{Time to recover to BMSY}
}
\description{
An alternative implementation of rebuildTime that works directly with FLPar objects.
This version uses a different forward projection approach with explicit surplus production calculations.
}
\examples{
# Create FLPar with required parameters
params=FLPar(r = 0.5, p = 1, bmsy = 500, fmsy = 0.2, virgin = 1000)
rebuild_time=rebuildTime3(params)

# With fishing mortality
rebuild_time=rebuildTime3(params, ftar = 0.5)
}
