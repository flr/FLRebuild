% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pMetricsFuncs.R
\name{kobePlot}
\alias{kobePlot}
\alias{ptK}
\title{Create Kobe Plot}
\usage{
kobePlot(data, xlim = c(0, 2), ylim = c(0, 2), add_lines = TRUE, ...)

ptK(...)
}
\arguments{
\item{data}{A data.frame with columns 'stock' (B/BMSY) and 'harvest' (F/FMSY),
or the result from kobeData()}

\item{xlim}{Numeric vector of length 2 for x-axis limits (default = c(0, 2))}

\item{ylim}{Numeric vector of length 2 for y-axis limits (default = c(0, 2))}

\item{add_lines}{Logical; if TRUE, adds reference lines at B/BMSY=1 and F/FMSY=1 (default = TRUE)}

\item{...}{Additional arguments passed to ggplot}
}
\value{
A ggplot object
}
\description{
Creates a Kobe plot (phase plot) showing stock status relative to reference points.
The plot shows B/BMSY vs F/FMSY with quadrant coloring.
}
\details{
The plot is divided into four quadrants:
- Green (top-left): Sustainable (B >= 1, F <= 1)
- Red (bottom-right): Collapsed (B < 1, F > 1)
- Yellow (bottom-left): Overfished only (B < 1, F <= 1)
- Orange (top-right): Overfishing only (B >= 1, F > 1)
}
\examples{
\dontrun{
# Create data
dtK <- kobeData(stock_rel, harvest_rel)

# Create plot
ptK <- kobePlot(dtK)
print(ptK)

# Or with custom limits
ptK <- kobePlot(dtK, xlim = c(0, 3), ylim = c(0, 3))
}

}
