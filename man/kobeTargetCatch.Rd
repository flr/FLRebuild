% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvln.R
\name{kobeTargetCatch}
\alias{kobeTargetCatch}
\title{Catch achieving a target probability by a given year}
\usage{
kobeTargetCatch(grid, year, target = 0.6, range = NULL)
}
\arguments{
\item{grid}{Output of \code{\link{kobeMomentGrid}}.}

\item{year}{Numeric year by which the target must be met.}

\item{target}{Target probability.}

\item{range}{Numeric vector of length two bounding the search in catch units;
defaults to the fitted catch range.}
}
\value{
Numeric catch level. \code{NA_real_} if the target is not met even at
  the lower bound of \code{range}; the upper bound if it is met throughout.
}
\description{
Inverts the interpolated probability surface to find the catch level at which
a stated probability is met by a stated year. This is the risk-equivalence
counterpart of \code{\link{kobeRebuildCurve}}: instead of asking when a catch
rebuilds the stock, it asks what catch rebuilds it by a chosen date.
}
\examples{
\dontrun{
grid <- kobeMomentGrid(runs, 2025:2070)
sapply(c(2040, 2050, 2070), function(y)
  kobeTargetCatch(grid, y, target = 0.60))
}
}
\seealso{
\code{\link{kobeExchangeRate}}
}
