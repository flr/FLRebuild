% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pella_tomlinson.R
\name{fastParams}
\alias{fastParams}
\alias{fastParams,FLPar,missing,missing-method}
\title{Fast parameter extraction utility}
\usage{
fastParams(msy, bmsy, virgin)

\S4method{fastParams}{FLPar,missing,missing}(msy, bmsy, virgin)
}
\arguments{
\item{msy}{Numeric vector or FLPar. Maximum Sustainable Yield.}

\item{bmsy}{Numeric vector or FLPar. Biomass at MSY.}

\item{virgin}{Numeric vector or FLPar. Virgin biomass/carrying capacity.}
}
\value{
If length 1 after recycling: named numeric vector (r, p, virgin, bmsy, msy).
Otherwise: data.frame with columns (r, p, virgin, bmsy, msy).
}
\description{
Quickly derives parameters from MSY, BMSY, and virgin biomass and returns them as numeric.
Vectorised over inputs. If the recycled length is 1, returns a named numeric vector; otherwise,
returns a data.frame with columns r, p, virgin, bmsy, msy.
}
\examples{
# Single set (returns named numeric vector)
fastParams(msy = 0.1, bmsy = 0.3, virgin = 1)

# Vectorised over virgin (returns data.frame)
fastParams(msy = 0.1, bmsy = 0.3, virgin = seq(1, 2, 0.1))

# With FLPar object
fastParams(FLPar(msy = 0.1, bmsy = 0.3, virgin = seq(1, 2, 0.1)))

}
