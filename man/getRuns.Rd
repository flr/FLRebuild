% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-generic.R, R/mvln.R
\name{getRuns}
\alias{getRuns}
\alias{getRuns,character-method}
\alias{getRuns,list-method}
\title{Load assessment or projection runs}
\usage{
getRuns(object, ...)

\S4method{getRuns}{character}(
  object,
  pattern = NULL,
  runs = NULL,
  source = c("auto", "ss", "jabba"),
  ...
)

\S4method{getRuns}{list}(object, runs = NULL, source = c("auto", "ss", "jabba"), ...)
}
\arguments{
\item{object}{Named character vector of paths, a directory, or a named list
of loaded run objects.}

\item{...}{Passed to methods (\code{pattern}, \code{runs}, \code{source}).}

\item{pattern}{Regular expression with one capture group for the run id
(catch level for SS; scenario label for JABBA) when \code{object} is a
directory.}

\item{runs}{Optional character vector of JABBA run labels to keep; ignored
for SS. \code{NULL} keeps all discovered JABBA runs.}

\item{source}{\code{"auto"}, \code{"ss"}, or \code{"jabba"}.}
}
\value{
A named list of run objects (SS_output-shaped or JABBA fits).
}
\description{
S4 generic for named lists of Stock Synthesis projection outputs or JABBA
assessment fits. \code{character} methods take a directory or a named vector
of paths; \code{list} methods validate an already-loaded collection.
Source is chosen with \code{source} (\code{"ss"}, \code{"jabba"}, or
\code{"auto"}). Auto-detection uses SS catch-level names / \code{*_\\{catch\\}t.Rdata}
files versus JABBA run labels / \code{*_jabba.rdata} files.
}
\examples{
\dontrun{
runs <- getRuns(c(
  "0" = "path/to/HW2e_..._0t.Rdata",
  "250" = "path/to/HW2e_..._250t.Rdata"))
runs <- getRuns("P:/.../SSoutput")
jb   <- getRuns("P:/.../outputCorrected", source = "jabba",
                runs = c("1-B", "1-S", "2-S"))
}
}
\seealso{
\code{\link{getJabbaRuns}}, \code{\link{kobeMomentGrid}},
  \code{\link{kobeMomentGridJabba}}, \code{\link{makeK2SM}}
}
